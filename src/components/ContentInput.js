import React, { Component } from 'react';

class ContentInput extends Component {
  render() {
    return (
      <textarea className="ContentTextArea"></textarea>
    );
  }
}

export default ContentInput;
